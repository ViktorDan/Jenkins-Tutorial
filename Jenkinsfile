pipeline {
  agent { label 'agent-1' }

  stages {
    stage('Setup') {
      steps {
        sh script: "", label: "Start of Pipeline"
        sh script: "echo "Hello World"", label: "Print Something"
      }
    }

    stage('build example') {
      steps {
        
      }
    }
}
